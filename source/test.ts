import { equal } from 'assert-helpers'
import kava from 'kava'

kava.suite('hooks', function (suite, test) {
	test('no tests yet', function () {
		console.log(
			'no tests yet, if you know how to test react hooks, then please assist'
		)
	})
})
